### Reindex Mac's Spotlight database

    $ sudo mdutil -i off /    # turn indexing off for the whole drive
    $ sudo mdutil -i on /     # turn indexing on again
    $ sudo mdutil -E /        # reindex 

    
    
